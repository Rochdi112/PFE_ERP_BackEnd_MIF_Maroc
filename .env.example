# Backend ENV (local development)

# Security
SECRET_KEY=change_me
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
FILES_ENC_KEY=your-fernet-key-here-replace-in-production

# Database (local Postgres if available; tests use SQLite memory automatically)
POSTGRES_USER=erp_user
POSTGRES_PASSWORD=erp_pass
POSTGRES_DB=erp_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# CORS (add your frontend URL here)
CORS_ALLOW_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# Uploads (local path, relative to project root)
UPLOAD_DIRECTORY=app/static/uploads

# Emails (dev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=user
SMTP_PASSWORD=password
EMAILS_FROM_EMAIL=no-reply@example.com

# Scheduler
ENABLE_SCHEDULER=false

# Notes:
# - For Docker, prefer using .env.docker.example (UPLOAD_DIRECTORY differs)
# - Validate with: python validate_env.py
## Core / Auth
PROJECT_NAME=ERP Interventions
SECRET_KEY=change-me
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Postgres (Docker)
POSTGRES_USER=erp_user
POSTGRES_PASSWORD=erp_pass
POSTGRES_DB=erp_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# URLs calculées par le code (ne pas éditer à la main)
# DATABASE_URL=postgresql+psycopg2://erp_user:erp_pass@db:5432/erp_db

# CORS
CORS_ALLOW_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# Scheduler (optional)
ENABLE_SCHEDULER=false

# Uploads
UPLOAD_DIRECTORY=app/static/uploads

# SMTP (for notifications)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=user
SMTP_PASSWORD=password
EMAILS_FROM_EMAIL=no-reply@example.com

# Ports hôtes
POSTGRES_HOST_PORT=5432
API_HOST_PORT=8000
